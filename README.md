# Astronomical-Object-Classification
End-to-end ML workflow that classifies SDSS astronomical objects using photometry, redshift, and derived color indices. Implements feature selection, four supervised models, and three clustering methods, with Random Forest achieving 97.65% accuracy.
